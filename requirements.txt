# Core — do NOT pin torch/torchaudio: Colab provides them pre-installed with CUDA.
# On Colab: pip install -r requirements.txt  (torch/torchaudio already present)
# Locally:  pip install torch torchaudio && pip install -r requirements.txt
torchaudio>=2.0.0
librosa>=0.10.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
